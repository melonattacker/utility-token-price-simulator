from simulation import simulation, generator
import matplotlib.pyplot as plt
import numpy as np

fig = plt.figure()

def main():
    sim = simulation.Simulation()
    # threshold, user_base = sim.calc_userbase_and_threshold(-2.0, 0.0, 1.0)
    # sim.calc_productivity()
    # print(sim.productivities)
    # plt.plot(sim.productivities)
    # fig.savefig("img.png")
    # print(threshold)
    # sim.calc_utility()
    # need = sim.calc_aggregate_transaction_need(sim.utilities[1], threshold)
    # print(need)
    # plt.plot(sim.utilities[1])
    # plt.show()

main()